jupyterhub:
  hub:
    services:
      binder:
        apiToken: "<apiToken>"
  proxy:
    secretToken: "<secretToken>"

registry:
  url: https://<acr-name>.azurecr.io
  username: <username>
  password: <password>

config:
  GitHubRepoProvider:
    access_token: <accessToken>
