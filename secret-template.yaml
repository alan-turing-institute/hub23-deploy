jupyterhub:
  hub:
    services:
      binder:
        apiToken: "<apiToken>"
  proxy:
    secretToken: "<secretToken>"
  config:
    GitHubRepoProvider:
      access_token: <accessToken>

registry:
  username: <docker-id>
  password: <password>
