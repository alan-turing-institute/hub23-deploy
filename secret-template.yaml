jupyterhub:
  hub:
    services:
      binder:
        apiToken: "<apiToken>"
  proxy:
    secretToken: "<secretToken>"

registry:
  username: <docker-id>
  password: <password>

config:
  GitHubRepoProvider:
    access_token: <accessToken>
